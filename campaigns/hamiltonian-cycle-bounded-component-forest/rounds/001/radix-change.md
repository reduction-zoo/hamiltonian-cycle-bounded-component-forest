# Representation refinement and retained incomplete executions

Before changing the candidate on 2026-09-22, revision f46ec6b preserves the original R=2 max(h_j)+1 implementation and proof. The prepared run had passed its first seven cases (14 target outputs), and the independent run had passed all 12 source graphs on at most three vertices (13 target outputs). Neither had reported a mismatch or UNKNOWN. A process-state snapshot measured elapsed times 5:13 and 2:33 respectively; these are observations, not imposed execution limits or evidence of eventual nontermination.

Inspection found the +1 in R unnecessary: every clause's full-item digit total is at most 2h_j-1, strictly below 2 max(h_j). The variable digit total is 2 and max(h_j)>=2 for n>=3, so the smaller radix also forbids all carries. Setting R=2 max(h_j) makes the radix a power of two and aligns the already-proved digit separation with binary arithmetic. This is a parameter refinement of the same mechanism, not a new research round or a change to the endpoints.

Plan: explicitly cancel both superseded executions before changing algorithm.py, preserving their partial outputs in progress.txt and here. Treat these cancellations as incomplete execution failures, never as NO-SOLUTION or completion of the finite test family. Rerun the same suites against the refined candidate. No solver or subprocess timeout is introduced. No correctness conclusion relies on an unfinished answer. Whether binary alignment improves completion is to be measured by the new actual runs; no speedup is asserted in advance.

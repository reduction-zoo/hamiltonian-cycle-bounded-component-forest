# Research campaign

Own this campaign while active. Follow `.agents/skills/research-session/SKILL.md`, `research/repository.md` and `research/reduction.md`. Fixed question: `campaigns/hamiltonian-cycle-bounded-component-forest/question.md`. Budget: 20 research rounds.

Use English, direct implementations and uv with a committed lockfile. Commit Prepare before constructing candidates. Prove polynomial-time F and G and recovery for every valid target output, including NO-SOLUTION, empty cases and arbitrarily large binary integers. Solver UNKNOWN and interruptions are execution failures, never NO. Preserve failures and incremental commits. No runtime sibling dependencies.

Read shared experience in place at `/Users/xiweipan/Codes/autoresearch-gadgets/research/experience/entries/`, without copying or modifying it. Other sessions own other lanes. Preserve unrelated edits. Do not change the board, publish, create remotes, post upstream or merge.

The latest user instruction supersedes queue continuation: finish ONLY this campaign, commit its closeout, then STOP. Do not initialize or research any next question. The user reports Fast OFF and service_tier="default" for the resumed process. Main researcher owns semantics, oracles and proofs. Mechanical delegation is optional and narrow; no estimated token savings. Obtain fresh-context independent review with the registered `.codex/agents/research-reviewer.toml`; no DSH preset handoff is required. Record actual model route, available measurements and enforcement limits. For an advance, write, compile and visually inspect the English Typst manuscript before stopping.
